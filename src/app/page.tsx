import { CircularProgress, LinearProgress } from '@mui/material';
import Image from 'next/image';

export default function Home() {
  return (
    <main>
      <h1>Hello This is no longer empty</h1>
      <LinearProgress color='secondary' />
    </main>
  );
}
