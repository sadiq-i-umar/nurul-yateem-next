import { Box, Typography } from "@mui/material";

const Nav:React.FC = () => {
    return (
        <Box>
            <Typography>This is the Nav</Typography>
        </Box>
    );
}

const NavItem:React.FC = () => {
    return (
        <Box>
            
        </Box>
    );
}

export default Nav;